import styled from '@emotion/styled'

import { ValidationInput } from '@/styles/globalStyles'

export const NumberField = styled(ValidationInput)`
  min-width: 8rem;
  max-width: 12rem;
  input {
    appearance: textfield;
  }
`
